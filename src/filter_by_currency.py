

# new_dict = {}
# # new_dict = ([k:v for k,v in transactions.items() if v["code"] == "USD"])
#
# for i in transactions:
#     for key in i:
#         print(key)
#
# filtered_list = list(filter(lambda x: x["operationAmount"]["currency"]["code"] == "USD", transactions))
#
# print(filtered_list)
